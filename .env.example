# STRMSync 环境变量配置

# ==================== 基础配置 ====================
# 时区
TZ=Asia/Shanghai

# 日志级别 (debug/info/warn/error)
LOG_LEVEL=info

# ==================== 端口配置 ====================
# 服务端口（前后端一体）
PORT=3000

# ==================== 安全配置 ====================
# 加密密钥（⚠️ 生产环境请务必修改为随机的 32 字符字符串）
ENCRYPTION_KEY=change-me-to-random-32-chars!!

# ==================== 路径映射 ====================
# 媒体源路径（宿主机路径）
# 可以挂载多个数据源，在容器内映射为 /media/source1、/media/source2 等
# 通过 Web 界面配置数据源时，使用容器内路径

# 示例 1: 本地 NAS
MEDIA_PATH_1=/volume1/Media

# 示例 2: CloudDrive2 挂载
MEDIA_PATH_2=/mnt/clouddrive

# 示例 3: OpenList 挂载（如果使用本地模式）
MEDIA_PATH_3=/mnt/openlist

# STRM 输出路径（宿主机路径）
# 生成的 STRM 文件和元数据将保存在这里
STRM_OUTPUT_PATH=/path/to/strm/output

# ==================== 说明 ====================
# 1. 所有业务配置（数据源、通知器等）通过 Web 界面管理
# 2. 路径映射示例：
#    宿主机: /volume1/Media/Movies
#    容器内: /media/source1/Movies
#    Web 配置源路径: /media/source1/Movies
#    Web 配置目标路径: /media/library/Movies
# 3. 如果不需要某个媒体源，可以注释掉对应的 MEDIA_PATH_X
