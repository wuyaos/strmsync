# STRMSync - 自动化 STRM 媒体文件管理系统

> 最后更新：2026-02-19 18:07:46
> 当前分支：refactor/architecture-v2 (Phase 2 架构重构)

## 项目概览

STRMSync 是一个自动化的 STRM 媒体文件管理系统，用于集成 CloudDrive2/OpenList 等云盘服务，自动生成 STRM 文件并同步到 Emby/Jellyfin/Plex 等媒体服务器。

**核心功能**：
- 🔄 自动监控云盘文件变化（本地模式/API 模式）
- 📝 自动生成 STRM 文件（支持多种 URL 格式）
- 🎬 自动同步到媒体服务器并触发刷新
- 📊 任务管理和执行记录追踪
- 🔌 多文件系统和媒体服务器支持

## 技术栈

### 后端
- **语言**：Go 1.24
- **框架**：Gin (HTTP)、GORM (ORM)
- **架构**：Clean Architecture (Transport → App → Domain → Infra)
- **数据库**：SQLite
- **日志**：Zap
- **集成**：
  - CloudDrive2 (gRPC h2c)
  - OpenList (HTTP)
  - Emby/Jellyfin (HTTP)

### 前端
- **框架**：Vue 3 (Composition API + `<script setup>`)
- **UI 库**：Element Plus
- **构建工具**：Vite 5
- **HTTP 客户端**：Axios
- **状态管理**：Pinia

## 架构说明

### Clean Architecture 分层

```
┌─────────────────────────────────────────────────────────┐
│  Transport Layer (HTTP Handlers)                        │
│  - job.go, task_run.go, filesystem_server.go, etc.     │
└────────────────┬────────────────────────────────────────┘
                 │
┌────────────────▼────────────────────────────────────────┐
│  Application Layer (Use Cases)                          │
│  - app/job/, app/taskrun/, app/sync/, app/file/        │
└────────────────┬────────────────────────────────────────┘
                 │
┌────────────────▼────────────────────────────────────────┐
│  Domain Layer (Business Logic)                          │
│  - model/models.go, model/uid.go                        │
└────────────────┬────────────────────────────────────────┘
                 │
┌────────────────▼────────────────────────────────────────┐
│  Infrastructure Layer (External Services)               │
│  - infra/filesystem/, infra/mediaserver/, infra/db/     │
└─────────────────────────────────────────────────────────┘
```

### 核心设计模式

1. **Provider 模式**：多文件系统支持（Local/CloudDrive2/OpenList）
2. **Adapter 模式**：多媒体服务器支持（Emby/Jellyfin）
3. **Repository 模式**：数据访问抽象
4. **Queue 模式**：任务队列和并发控制

## 目录结构

```
strm/
├── backend/                    # Go 后端
│   ├── cmd/server/            # 服务器入口
│   │   └── main.go            # 主程序入口 (路由注册、依赖注入)
│   ├── internal/
│   │   ├── app/               # 应用层 (Use Cases)
│   │   │   ├── job/           # 任务管理用例
│   │   │   ├── taskrun/       # 执行记录用例
│   │   │   ├── sync/          # 同步用例
│   │   │   └── file/          # 文件操作用例
│   │   ├── domain/            # 领域层
│   │   │   └── model/         # 领域模型
│   │   ├── infra/             # 基础设施层
│   │   │   ├── filesystem/    # 文件系统集成 (Provider 模式)
│   │   │   ├── mediaserver/   # 媒体服务器集成 (Adapter 模式)
│   │   │   └── db/            # 数据库配置和迁移
│   │   ├── transport/         # 传输层 (HTTP Handlers)
│   │   │   ├── job.go         # 任务管理 API
│   │   │   ├── task_run.go    # 执行记录 API
│   │   │   ├── filesystem_server.go  # 数据服务器 API
│   │   │   ├── media_server.go       # 媒体服务器 API
│   │   │   └── server_types.go       # 服务器类型定义 API
│   │   ├── worker/            # 后台任务执行器
│   │   ├── scheduler/         # 任务调度器
│   │   ├── queue/             # 任务队列
│   │   ├── strmwriter/        # STRM 文件生成器
│   │   └── pkg/               # 公共包
│   │       ├── logger/        # 日志工具
│   │       └── sdk/           # 第三方 SDK
│   ├── go.mod
│   └── go.sum
├── frontend/                   # Vue 3 前端
│   ├── src/
│   │   ├── views/             # 页面组件
│   │   │   ├── Jobs.vue       # 任务管理页面
│   │   │   ├── TaskRuns.vue   # 运行记录页面
│   │   │   ├── Servers.vue    # 服务器管理页面
│   │   │   ├── Dashboard.vue  # 仪表盘
│   │   │   ├── Logs.vue       # 日志查看
│   │   │   └── ConfigManagement.vue  # 配置管理
│   │   ├── api/               # API 客户端
│   │   │   ├── jobs.js        # 任务 API
│   │   │   ├── runs.js        # 运行记录 API
│   │   │   ├── servers.js     # 服务器 API
│   │   │   ├── request.js     # Axios 配置
│   │   │   └── normalize.js   # 响应标准化
│   │   ├── router/            # 路由配置
│   │   ├── layouts/           # 布局组件
│   │   └── main.js            # 入口文件
│   ├── package.json
│   └── vite.config.js
├── docs/                       # 技术文档
│   ├── README.md              # 文档索引
│   ├── HTTP_API.md            # HTTP API 参考
│   └── DEPLOYMENT.md          # 部署指南
├── scripts/                    # 开发脚本
│   ├── dev-start.sh           # 启动后端服务
│   ├── dev-stop.sh            # 停止后端服务
│   ├── dev-restart.sh         # 重启后端服务
│   └── README.md              # 脚本使用说明
└── DEVELOPMENT.md              # 开发说明
```

## 模块导航

### 后端核心模块

| 模块 | 路径 | 说明 | 关键文件 |
|------|------|------|----------|
| **服务器入口** | `backend/cmd/server/` | HTTP 服务器启动和路由注册 | `main.go:230-337` |
| **任务管理** | `backend/internal/app/job/` | 任务 CRUD 和生命周期管理 | - |
| **执行记录** | `backend/internal/app/taskrun/` | 任务执行记录管理 | - |
| **同步引擎** | `backend/internal/app/sync/` | 文件同步和 STRM 生成逻辑 | - |
| **文件系统** | `backend/internal/infra/filesystem/` | 多文件系统集成 (Provider 模式) | - |
| **媒体服务器** | `backend/internal/infra/mediaserver/` | 多媒体服务器集成 (Adapter 模式) | - |
| **HTTP API** | `backend/internal/transport/` | HTTP 处理器和路由 | `job.go`, `task_run.go` |
| **Worker** | `backend/internal/worker/` | 后台任务执行器 | - |
| **Scheduler** | `backend/internal/scheduler/` | 任务调度器 | - |
| **Queue** | `backend/internal/queue/` | 任务队列 | - |

### 前端核心模块

| 模块 | 路径 | 说明 | 关键文件 |
|------|------|------|----------|
| **任务管理** | `frontend/src/views/Jobs.vue` | 任务列表、创建、编辑、触发 | - |
| **运行记录** | `frontend/src/views/TaskRuns.vue` | 执行记录列表、详情、统计 | - |
| **服务器管理** | `frontend/src/views/Servers.vue` | 数据服务器和媒体服务器管理 | - |
| **API 客户端** | `frontend/src/api/` | HTTP 请求封装 | `jobs.js`, `runs.js`, `servers.js` |

## HTTP API 端点

### Jobs API (任务管理)
- `POST /api/jobs` - 创建任务
- `GET /api/jobs` - 获取任务列表
- `GET /api/jobs/:id` - 获取任务详情
- `PUT /api/jobs/:id` - 更新任务
- `DELETE /api/jobs/:id` - 删除任务
- `POST /api/jobs/:id/run` - 手动触发任务
- `POST /api/jobs/:id/stop` - 停止任务
- `PUT /api/jobs/:id/enable` - 启用任务
- `PUT /api/jobs/:id/disable` - 禁用任务

### Runs API (执行记录)
- `GET /api/runs` - 获取执行记录列表
- `GET /api/runs/:id` - 获取执行记录详情
- `POST /api/runs/:id/cancel` - 取消运行中的任务
- `GET /api/runs/stats` - 获取运行统计信息

### Servers API (服务器管理)
- `GET /api/servers/data` - 获取数据服务器列表
- `POST /api/servers/data` - 创建数据服务器
- `GET /api/servers/data/:id` - 获取数据服务器详情
- `PUT /api/servers/data/:id` - 更新数据服务器
- `DELETE /api/servers/data/:id` - 删除数据服务器
- `POST /api/servers/data/:id/test` - 测试数据服务器连接
- `GET /api/servers/media` - 获取媒体服务器列表
- `POST /api/servers/media` - 创建媒体服务器
- `GET /api/servers/media/:id` - 获取媒体服务器详情
- `PUT /api/servers/media/:id` - 更新媒体服务器
- `DELETE /api/servers/media/:id` - 删除媒体服务器
- `POST /api/servers/media/:id/test` - 测试媒体服务器连接
- `GET /api/servers/types` - 获取服务器类型定义列表
- `GET /api/servers/types/:type` - 获取单个服务器类型定义

详细 API 文档请参考：[docs/HTTP_API.md](docs/HTTP_API.md)

## 开发指南

### 环境要求
- Go 1.24+
- Node.js 18+
- SQLite 3

### 快速开始

**后端开发**：
```bash
cd backend
go mod download
go run cmd/server/main.go
```

**前端开发**：
```bash
cd frontend
npm install
npm run dev
```

**使用开发脚本**：
```bash
# 启动后端服务（后台运行）
./scripts/dev-start.sh

# 停止后端服务
./scripts/dev-stop.sh

# 重启后端服务
./scripts/dev-restart.sh
```

### 测试

**后端测试**：
```bash
cd backend
go test ./...
```

**前端测试**：
```bash
cd frontend
npm run test
```

### 代码规范

**后端**：
- 遵循 Clean Architecture 分层原则
- 使用 Provider/Adapter 模式实现多服务支持
- 所有 HTTP Handler 必须进行参数验证和错误处理
- 使用 context 管理请求生命周期
- 日志使用 Zap 结构化日志

**前端**：
- 使用 Vue 3 Composition API + `<script setup>`
- 组件命名使用 PascalCase
- API 调用统一使用 `src/api/` 模块
- 错误处理统一在 Axios 拦截器中处理
- 使用 Element Plus 组件库

## 最近更新

### Phase 2 架构重构 (2026-02-19)

**已完成**：
- ✅ Clean Architecture 分层重构
- ✅ Provider 模式实现多文件系统支持
- ✅ Adapter 模式实现多媒体服务器支持
- ✅ 前端 Composition API 全面应用
- ✅ HTTP API 前后端对齐
- ✅ 任务队列和调度器实现
- ✅ 完整的错误处理和日志记录

**最近提交**：
- `27b93a9` - feat(api): 对齐前后端 API 端点
- `a3363e5` - feat(model): 完善领域模型和 UID 生成
- `55af8f3` - feat(transport): 完善 HTTP API 处理器
- `38fc04f` - feat(frontend): 添加新的 API 客户端模块

**待完成**：
- ⏳ 前端单元测试（建议使用 Vitest）
- ⏳ E2E 测试套件（建议使用 Playwright）
- ⏳ Docker 部署方案完善
- ⏳ 性能优化和监控

## 相关文档

- [HTTP API 参考](docs/HTTP_API.md)
- [部署指南](docs/DEPLOYMENT.md)
- [开发脚本说明](scripts/README.md)
- [项目开发说明](DEVELOPMENT.md)

## 贡献指南

1. Fork 项目
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'feat: Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 许可证

本项目采用 MIT 许可证。详见 LICENSE 文件。

---

**注意**：本文档由 AI 自动生成和维护，最后更新时间：2026-02-19 18:07:46
